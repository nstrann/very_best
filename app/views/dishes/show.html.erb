<div class="row justify-content-center mb-3">
  <div class="col-md-8">
    <%= render object: @dish, partial: "card", as: :dish %>
  </div>
</div>

  <!-- Show Page Customization starts -->
<div class="row justify-content-center mb-3">
  <div class="col-md-8">
    <%= render partial: "dishes/dish_bookmarks" %>
  </div>
</div>
  <!-- Show Page Customization end -->
