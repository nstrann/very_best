<%= form_with(model: @dish, local: true) do |f| %>

  <div class="form-group">
    <%= f.label :dish_name %>

    <%= f.text_field :dish_name, class: "form-control" %>
  </div>

  <%= f.hidden_field :c_id, value: @cuisine.id %>

  <%= f.submit "Create dish", class: "btn btn-block btn-outline-secondary" %>
<% end %>
